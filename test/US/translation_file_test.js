const {
  home, h1, h2, kids, asSeenOn, howItWorks,
  support, ourStory, ambassadors, wholesale
} = require("../../values/pageUrls");

require("../translation_file_test")("US",
  [
    home, h1, h2, kids, asSeenOn, howItWorks,
    support, ourStory, ambassadors, wholesale
  ]
);
